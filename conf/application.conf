# This is the main configuration file for the application.
# https://www.playframework.com/documentation/latest/ConfigFile

include "oidc"

# Secrets are loaded from a gitignored file at project root: .env.oauth.local
# or from process environment variables at runtime by the Auth controller.

# HTTP server port
play.server.http.port = 11000